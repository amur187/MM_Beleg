//PFont font;
//String time = "010";
//int t;
//int interval = 10;

/*
Spiel spiel = new Spiel();

void setup()
{
  Spieler a = new Spieler("Alihan");
  Spieler b = new Spieler("Dennis");
  Spieler c = new Spieler("Paul"); 

  spiel.spielerliste.spielerHinzufuegen(a);
  spiel.spielerliste.spielerHinzufuegen(b);
  spiel.spielerliste.spielerHinzufuegen(c);
  
  spiel.spielStarten();
  println(spiel.getRunde());
  println(spiel.spielerliste.getAktuellerSpieler().getName() + " " +spiel.getWortDerRunde() +" "+spiel.spielerliste.getAktuellerSpieler().getPunkte());
  spiel.spielerWechsel();
  println(spiel.spielerliste.getAktuellerSpieler().getName()+ " " +spiel.getWortDerRunde());
  spiel.spielerWechsel();
  println(spiel.spielerliste.getAktuellerSpieler().getName()+ " " +spiel.getWortDerRunde());
  spiel.naechsteRunde();
  println(spiel.spielerliste.getAktuellerSpieler().getName()+ " " +spiel.getWortDerRunde());
}

void draw()
{
  // background(255);

  // t = interval-int(millis()/1000);
  // time = nf(t , 3);
  // if(t == 0){
  //   println("GAME OVER");
  //   interval+=10;
  // }

  //text(time, width/2, height/2);
} */
